{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test:unit": "vitest --run --reporter verbose --globals",
    "test:watch": "vitest --reporter verbose --globals"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.1.3",
    "@testing-library/vue": "^6.6.0",
    "nuxt": "3.0.0-rc.4",
    "vitest": "^0.18.0"
  }
}
